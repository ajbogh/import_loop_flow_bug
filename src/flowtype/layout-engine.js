// @flow
import { type GraphViewProps } from './graph-view';

export class LayoutEngine {
  graphViewProps: GraphViewProps;
  constructor(graphViewProps: GraphViewProps) {
    this.graphViewProps = graphViewProps;
  }
}
